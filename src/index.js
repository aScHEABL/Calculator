import "./style.css";

const operatorArray = [`+`, `-`, `*`, `/`];
const functionArray = [`=`, `AC`, `backspace`, `M`, `sign`];
const numButtons_DOM = document.querySelectorAll("[data-number-button]");

console.log(numButtons_DOM);